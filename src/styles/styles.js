import { StyleSheet } from 'react-native';
const styles = StyleSheet.create(
    {
      letraGordita: {
        fontSize: 20,
        fontWeight: 'bold'
      },
      letra: {
        fontSize: 20,
        color: 'black',
      },
      view: {
        padding: 10
      },
      informacion: {
        textAlign: 'center',
        padding: 10,
        fontWeight: 'bold',
        fontSize: 20,
        color: 'black',
      }
    }
  )
export default styles;